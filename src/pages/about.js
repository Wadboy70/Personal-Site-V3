import React from 'react';
import Layout from '../components/layout';

const About = (props) => {
    console.log(props);
    return(
        <Layout>
            <div>

            </div>
        </Layout>
    );
};

export default About;